# Server Configuration
PORT=8080
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=tempaskill

# JWT Configuration
# CRITICAL: Generate a strong random secret (minimum 32 characters)
# Generate with: openssl rand -base64 64
# DO NOT use a weak or predictable secret in production!
JWT_SECRET=
JWT_EXPIRATION_HOURS=24

# Firebase Configuration
# Path to Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT=ngecis-4035d-firebase-adminsdk-fbsvc-b062d03ee9.json
# Firebase Storage bucket name
FIREBASE_STORAGE_BUCKET=ngecis-4035d.firebasestorage.app

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000
